# ==========================================================
# M3 Default Config (Unified Runtime)
#
# Runtime 固定値:
# - step_ms = 40
# - chunk_len_ms = 400
# - overlap_ms = 200
#
# これらは contracts_runtime_session_v0.2 により固定。
# 変更してはならない。
#
# mouth / expression は audio_ms に clamp 必須。
#
# praat-parselmouth が未導入の場合は実行不可。
# ==========================================================
